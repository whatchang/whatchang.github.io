---
import ThemeToggle from "./ThemeToggle.astro";
import { profile } from "../data/profile";

const sections = [
  { id: "about", label: "About" },
  { id: "research", label: "Research" },
  { id: "featured", label: "Featured" },
  { id: "publications", label: "Publications" },
  { id: "news", label: "News" },
];
---

<nav
  class="sticky top-0 z-40 border-b border-zinc-200 bg-white/80 backdrop-blur dark:border-zinc-800 dark:bg-zinc-950/80"
>
  <div class="mx-auto flex max-w-3xl items-center justify-between px-6 py-3">
    <a
      href="#top"
      class="text-sm font-semibold tracking-tight text-zinc-900 hover:text-indigo-600 dark:text-zinc-100 dark:hover:text-indigo-400"
    >
      {profile.nameEn}
    </a>

    <div class="flex items-center gap-1 sm:gap-2">
      <ul class="hidden items-center gap-1 text-sm sm:flex">
        {
          sections.map((s) => (
            <li>
              <a
                href={`#${s.id}`}
                class="rounded-md px-2 py-1 text-zinc-600 transition hover:bg-zinc-100 hover:text-zinc-900 dark:text-zinc-400 dark:hover:bg-zinc-900 dark:hover:text-zinc-100"
              >
                {s.label}
              </a>
            </li>
          ))
        }
      </ul>
      <ThemeToggle />
    </div>
  </div>
</nav>
